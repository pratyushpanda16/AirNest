# ===== PRODUCTION PROFILE =====

# DB Configuration (constructed from Render's individual DB properties)
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# Hibernate - use 'update' for initial deployment, switch to 'validate' once stable
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Connection pool (optimized for Render free tier)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2

# JWT
jwt.secretKey=${JWT_SECRET}

# Frontend URL
frontend.url=${FRONTEND_URL}

# Stripe (app starts with placeholders; set real keys in Render dashboard for payments to work)
stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_placeholder}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_placeholder}

# Server
server.servlet.context-path=/api/v1
server.port=8080

# Actuator - expose health endpoint only
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized

# Swagger
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha

# Logging
logging.level.root=WARN
logging.level.com.aman.AirBnb=INFO
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
